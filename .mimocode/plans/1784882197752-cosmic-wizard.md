# Plan: Fix All Configuration & Code Quality Issues

## Overview

38 issues identified across the TAPaware repo — 4 critical, 8 high, 18 medium, 8 low.
Organized into 7 execution batches by dependency order and logical grouping.

---

## Batch 1: Docker Infrastructure (Critical #1-4)

### 1a. Fix `docker-compose.infra.yml` — Network + Postgres init
**File:** `tap-app/docker-compose.infra.yml`
- Add explicit `networks:` section with `name: tap-net` (line ~209)
- Add `init-scripts/create-hydra-db.sh` — a small init script that creates the `hydra` database via `psql`
- Mount it into postgres: `./init-scripts:/docker-entrypoint-initdb.d`
- Add healthcheck to zookeeper (line ~63)
- Add `condition: service_healthy` to temporal's depends_on (line 143)

### 1b. Fix `docker-compose.app.yml` — Network + depends_on + env
**File:** `tap-app/docker-compose.app.yml`
- Change network name from `hybrid_default` to `tap-net` (line 141)
- Add `depends_on` with healthcheck conditions to all 5 services
- Remove `OAUTHLIB_INSECURE_TRANSPORT: 1` (line 58)
- Remove redundant `.env` volume mount (line 27) — `env_file` already handles it

### 1c. Fix `Dockerfile` — Error handling + non-root user
**File:** `tap-app/Dockerfile`
- Line 23: Remove `2>/dev/null || true` — let pip errors propagate
- Add `RUN useradd -m appuser && USER appuser` after pip install in base stage
- Add `.dockerignore` entry for `scripts/` or only COPY needed scripts

### 1d. Fix `Dockerfile.frontend` — nginx.conf
**File:** `tap-app/Dockerfile.frontend`
- Create a minimal `frontend/nginx.conf` for the prod target
- Or: remove the prod target until nginx config is ready, and document it

### 1e. Fix `debezium.ps1` — Kafka port
**File:** `tap-app/scripts/debezium.ps1`
- Line 80: Change `kafka:9092` to `kafka:29092`

---

## Batch 2: Python Package Config (High #10-12, Medium #16-23)

### 2a. Fix root `pyproject.toml`
**File:** `pyproject.toml`
- Add `optional-dependencies`: `embeddings = ["sentence-transformers>=2.2.0"]`, `vss = ["sqlite-vss>=0.1.0"]`, `yaml = ["pyyaml>=6.0"]`
- Move `sentence-transformers` from hard deps to `embeddings` optional
- Add `[tool.mypy]` section (basic: `python_version = "3.10"`, `warn_return_any = true`)
- Add `[tool.ruff.format]` section
- Add `readme`, `license`, `authors` metadata

### 2b. Fix root `src/aware/__init__.py`
**File:** `src/aware/__init__.py`
- Add `__version__ = "0.1.0"`
- Add `RemoteEmbeddingService` to imports and `__all__`
- Add lazy `__getattr__` for heavy optional imports

### 2c. Fix root `src/aware/config.py`
**File:** `src/aware/config.py`
- Change `llm_api_key` and `embedding_api_key` to `Optional[SecretStr]`
- Add `llm_api_base: Optional[str] = None`
- Add `model_post_init` for `embedding_api_key` fallback to `llm_api_key`

### 2d. Create root `.env.example`
**New file:** `.env.example`
- Document all `AWARE_*` environment variables

---

## Batch 3: Runtime Safety (High #10-12)

### 3a. Replace `assert` with runtime checks
**Files:**
- `src/aware/api/engine_hooks.py` lines 183,189,193,199: `assert self.expansion is not None` → `if self.expansion is None: raise RuntimeError("AwareEngine not initialized")`
- `src/aware/memory/database.py` lines 169,173,177,182,187: `assert self.conn is not None` → `if self.conn is None: raise RuntimeError("Database not initialized")`

### 3b. Fix blocking I/O in async functions
**Files:**
- `src/aware/memory/embeddings.py` lines 37,45: Wrap `self._model.encode()` in `asyncio.to_thread()`
- `src/aware/memory/knowledge_expansion.py` lines 285,317,323,360,384: Wrap `open()`/`json.load()`/`yaml.safe_load()` in `asyncio.to_thread()` or use `aiofiles`
- `src/aware/memory/persistence.py` lines 137,142: Same pattern

### 3c. Fix silent exception swallowing
**Files:**
- `src/aware/context/compressor.py` lines 119-120: Add `logging.exception("LLM summarization failed")` before `return None`
- `src/aware/memory/vector_store.py` lines 59-60: Narrow to `except (ImportError, sqlite3.OperationalError)`
- `src/aware/memory/consolidation.py` lines 196-197: Add `logging.warning("Embedding failed, falling back to no-clustering")`
- `tap-app/src/tap/config.py` line 283: Add `logging.warning("Failed to persist env vars: %s", e)`

---

## Batch 4: tap-app Config (Medium #13-15, #24-30)

### 4a. Fix `tap-app/pyproject.toml`
**File:** `tap-app/pyproject.toml`
- Fix testpaths duplication: remove specific subdirs, keep only `"tests"`
- Remove `pytest-postgresql` from dev deps (or document `-p no:postgresql` requirement)
- Remove `mypy-protobuf` from dev deps (dead)
- Fix version to `3.1` (align with docs)

### 4b. Fix `tap-app/src/tap/__init__.py`
**File:** `tap-app/src/tap/__init__.py`
- Update docstring from "v2.2" to "v3.1"
- Update `__version__` from "2.3.0" to "3.1.0"

### 4c. Fix `tap-app/src/tap/config.py` — env var naming
**File:** `tap-app/src/tap/config.py`
- Add `log` import and logging to `save_env_vars` exception handler
- Document that env var names follow pydantic-settings uppercase convention

### 4d. Complete `tap-app/.env.example`
**File:** `tap-app/.env.example`
- Add all 29 missing Settings fields with comments
- Remove `AWARE_*` entries (cross-package leakage)

### 4e. Fix `tap-app/.gitignore`
**File:** `tap-app/.gitignore`
- Add `node_modules/`
- Add `mypy_cache/`
- Add `data/server.log*`
- Add `data/*.json`

### 4f. Delete duplicate packages
**Delete:** `tap-app/src/aware_memory/` (17 files), `tap-app/src/aware_context/` (5 files)
- These are orphaned copies never imported by any tap-app code

---

## Batch 5: Code Deduplication (Medium #11.3, #11.2)

### 5a. Extract `_row_to_unit` to `models.py`
**File:** `src/aware/memory/models.py`
- Add `@classmethod from_row(cls, row) -> MemoryUnit` on `MemoryUnit`
- Update all 8 memory modules to use `MemoryUnit.from_row(row)` instead of local `_row_to_unit`

### 5b. Extract `_cosine_similarity` to shared utility
**File:** `src/aware/memory/vector_store.py` (or new `utils.py`)
- Move `_cosine_similarity` to a single location
- Update `consolidation.py` to import from shared location

### 5c. Fix double search in `get_attack_types_for_probe`
**File:** `src/aware/memory/knowledge_expansion.py` line 465
- Store result of `search_attack_types` in a variable, use it once

### 5d. Fix `setattr` in `update_attack_type`
**File:** `src/aware/memory/knowledge_expansion.py` line 110-112
- Use Pydantic `model_copy(update=...)` instead of `setattr` loop

---

## Batch 6: Minor Fixes (Low #31-38)

### 6a. Root `pyproject.toml` — ruff config
- Add `line-length = 120` or remove `E501` from ignore

### 6b. `src/aware/context/monitor.py` — dataclass → Pydantic
- Change `ContextStatus` from `@dataclass` to Pydantic `BaseModel`

### 6c. `src/aware/api/schemas.py` — add basic tests
- Create `tests/test_schemas.py` with validation tests

### 6d. `src/aware/config.py` — add basic tests
- Create `tests/test_config.py` with env var loading tests

### 6e. Add `__version__` test
- Test that `aware.__version__` is accessible

### 6f. Fix `MemoryManager.__init__` type signature
**File:** `src/aware/memory/manager.py`
- Change `config: Optional[AwareConfig | str] = None` to match actual usage

### 6g. Fix `stream_listener.py` redundant save
**File:** `tap-app/src/tap/stream_listener.py`
- Line 256-259: Remove duplicate `save_env_vars` call (lowercase key version)

---

## Batch 7: Verification

### 7a. Run tests
```bash
python -m pytest tests/ -v -p no:postgresql
```

### 7b. Syntax check
```bash
python -c "import ast, glob; [ast.parse(open(f).read()) for f in glob.glob('src/**/*.py', recursive=True)]"
```

### 7c. Grep for remaining issues
- `from memory.` (should be `from aware.memory.`)
- `assert self.` in aware package (should be `if ... raise`)
- `except Exception:\n\s*pass` in aware package
- `sys.path.insert` in aware package

### 7d. Docker build test
```bash
cd tap-app && docker compose -f docker-compose.infra.yml config
cd tap-app && docker compose -f docker-compose.app.yml config
```

---

## Files Modified (summary)

| Batch | Files | New |
|-------|-------|-----|
| 1 | `docker-compose.infra.yml`, `docker-compose.app.yml`, `Dockerfile`, `Dockerfile.frontend`, `debezium.ps1` | `init-scripts/create-hydra-db.sh`, `frontend/nginx.conf` |
| 2 | `pyproject.toml`, `src/aware/__init__.py`, `src/aware/config.py` | `.env.example` |
| 3 | `engine_hooks.py`, `database.py`, `compressor.py`, `vector_store.py`, `consolidation.py`, `embeddings.py`, `knowledge_expansion.py`, `persistence.py` | — |
| 4 | `tap-app/pyproject.toml`, `tap-app/src/tap/__init__.py`, `tap-app/src/tap/config.py`, `tap-app/.env.example`, `tap-app/.gitignore` | — |
| 5 | `models.py`, `vector_store.py`, `consolidation.py`, `knowledge_expansion.py` + 8 memory modules | — |
| 6 | `pyproject.toml`, `context/monitor.py`, `manager.py`, `stream_listener.py` | `tests/test_schemas.py`, `tests/test_config.py` |
| **Total** | **~30 files modified** | **~5 new files** |

## Execution Order

Batches 1-2 are independent (Docker vs Python). Batch 3 depends on 2c (SecretStr).
Batch 4 is independent. Batch 5 depends on nothing. Batch 6 depends on nothing.
Batch 7 runs after all others.

Recommended: Run batches 1+2 in parallel, then 3+4+5+6, then 7.
